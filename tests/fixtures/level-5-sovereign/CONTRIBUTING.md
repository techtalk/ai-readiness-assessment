# Contributing — wordcount

Same spec-first rules as L4. Plus: every load-bearing decision must
land as a story in `CHOICES.md`. The governance audit (quarterly)
checks for drift.

See `CLAUDE.md` for the day-to-day workflow.
